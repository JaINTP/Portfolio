{
  "buildCommand": "cd apps/portfolio && npm install --legacy-peer-deps && next build",
  "outputDirectory": "apps/portfolio/.next",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://api.jaintp.com/api/:path*"
    }
  ]
}
